package model

{{if or .NeedsTime .NeedsGorm}}
import (
{{- if .NeedsTime}}
	"time"
{{- end}}
{{- if .NeedsGorm}}
	"gorm.io/gorm"
{{- end}}
)
{{end}}

// Code generated by microgen. DO NOT EDIT.

// {{.Model.Name}}{{if .Model.Comment}} - {{.Model.Comment}}{{end}}
type {{.Model.Name}} struct {
{{- range .Model.Fields}}
	{{.Name}} {{.Type}}{{if or .JSONTag .GormTag}} `{{if .JSONTag}}json:"{{.JSONTag}}"{{end}}{{if and .JSONTag .GormTag}} {{end}}{{if .GormTag}}gorm:"{{.GormTag}}"{{end}}`{{end}}{{if .Comment}} // {{.Comment}}{{end}}
{{- end}}
{{- if .AddAuditFields}}
	CreatedAt time.Time      `json:"created_at" gorm:"autoCreateTime"`
	UpdatedAt time.Time      `json:"updated_at" gorm:"autoUpdateTime"`
	DeletedAt gorm.DeletedAt `json:"deleted_at,omitempty" gorm:"index"`
{{- end}}
}

// TableName specifies the gorm table name.
func ({{.Model.Name}}) TableName() string {
	return "{{.Model.TableName}}"
}
